{
  "name": "metin2-event-bot",
  "version": "1.0.0",
  "description": "Metin2 Event Bot for Discord",
  "main": "api/webhook.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step required'",
    "start": "node api/webhook.js"
  },
  "dependencies": {
    "node-cron": "^3.0.3"
  },
  "engines": {
    "node": "18.x"
  }
}
